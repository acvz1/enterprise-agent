# 企业知识库 Agent 协作约定

本文件用于跨会话和上下文压缩恢复协作方式。处理本项目之前，先读取本文件；涉及进度、排期或下一步时，再读取 `docs/04-six-day-core-plan.md`。

## 1. 开发所有权

- 用户是核心代码的实现者，目标是亲手完成项目并准备面试。
- 默认流程：助手说明需求、原因、精确文件/方法、完成标准；用户编码；助手读取并审查；通过后继续下一步。
- “阅读、解释、检查、诊断、继续”不授权助手修改核心业务代码。
- 只有用户明确说“你来修改、直接写、帮我修复”等表达时，助手才编辑代码；编辑前先说清最小范围。
- 原项目遗留的非核心机械修正可在用户明确交给助手后代为完成，但不得顺手扩大重构范围。

## 2. 消息语义

- 用户提出问题时，先回答当前问题，不擅自推进或改代码。
- 用户发送“OK、好、理解、可以、完成”等非疑问式进度确认时，默认上一编码步骤已经完成并保存；助手立即读取代码、验收并推进，不要求用户重复说“完成”。
- 非疑问消息若明显是在修改目标、补充规则或报告状态，应执行最新指令，而不是机械当作完成确认。

## 3. 讲解深度

- 不主动解释普通 Java 语法、getter/setter、常见循环和格式；用户主动问时再解释。
- 重点解释陌生第三方包 API、项目自定义调用链、Spring 行为、数据输入输出和工程取舍。
- 第一次出现的英文类名、字段名和缩写给出英文拆解与中文含义，例如 `RetrievalCandidate = Retrieval（检索）+ Candidate（候选项）`。
- 对第三方 API 说明：对象是什么、方法做什么、关键参数、返回值、在当前链路中的作用。不要只说“框架自动完成”。
- 用户追问时只补当前缺口；已明确理解的内容不重复展开。

## 4. 思考与决策

- 不因为用户提出一个技术名词就盲目照做。先从最终消费者、返回结果、数据权威性、查询次数、一致性和面试证据判断方案。
- 当用户理解存在偏差时，用当前代码和数据流说明，不做无依据的顺从。
- 区分“修复准确性”“性能优化”“架构分层”和“对外接口需求”，记录方案从错误假设到最终设计的演进。
- 计划中的功能只有实现并验证后才能称为项目竞争力或写进简历。

## 5. 笔记约定

- 主学习笔记：`C:\Users\Administrator\Desktop\企业知识库Agent项目学习笔记.md`。
- 已建立自动记笔记约定：完成一条连贯、可运行或可完整讲清的数据链路后，统一更新笔记，不必等待用户再次说“记入笔记”。
- 应记录：关键架构与数据流、重要第三方 API、真实错误及根因、方案演进、性能/一致性取舍、验证结果、可用的面试话术与追问。
- 不记录：普通语法、纯格式调整、重复结论、尚未验证的设想。
- 更新前完整读取现有笔记；保留用户措辞、编号、结构和无关内容。笔记只写已确认事实，并区分“已经验证”和“后续计划”。

## 6. 连续性与进度

- 当前唯一排期基线：`docs/04-six-day-core-plan.md`。
- 不再用 `docs/03-secondary-development-roadmap.md` 中 25～38 天的旧生产级估算判断当前进度。
- 进度必须按计划中的当天完整验收成果计算；子任务完成度不能冒充当天整体完成度。
- 编译成功不等于运行链路完成；需要 Redis、Elasticsearch、MySQL 或 HTTP 的功能必须有实际运行证据。
- 上下文丢失后先读本文件、六天计划和学习笔记的相关最新章节，再继续；不要凭记忆重建计划。

## 7. 当前固定方向

```text
Redis 向量召回 RetrievalCandidate
  + Elasticsearch BM25 召回 RetrievalCandidate
  -> RRF 排名融合
  -> 选择 Top K
  -> MySQL 批量补全权威数据
  -> RetrievalHit
  -> RAG / Agent 使用 chunk 证据回答并返回引用
```

- MySQL 是权威数据源；Redis 与 Elasticsearch 是可重建检索索引。
- 当前六天范围不包含制度审查、MCP、Reranker、完整上传系统重写、生产级复杂 ACL 或大规模前端改版。
- 不得在没有用户新决定的情况下改变这个产品方向。
